const User=require('../models/user');
const timedMeal=require('../models/timedMeal');
const dayPlan=require('../models/dayPlan');
const weekPlan=require('../models/weekPlan');

const configureMealPlan=(req,res)=>{
    try{

    }catch(err){
        
    }
}